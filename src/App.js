/*
 * Application logic to run on a node
 */

'use strict';

/**
 * App
 *
 * @constructor
 * @param {String} id
 * @return {undefined}
 */
var App = function(id) {
    this.id = id;
};

module.exports = App;
